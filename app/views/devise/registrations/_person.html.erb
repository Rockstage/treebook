<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { multipart: true, :method => :put, class: 'form-horizontal', "data-ajax" => "false" }) do |f| %>

  <div class="col-md-6">
  <fieldset class="form-group">
    <legend>About Me</legend>
    <%= f.input :first_name, input_html: { class: 'form-control' } %>
    <%= f.input :last_name, input_html: { class: 'form-control' } %>
    <%= f.input :birthday, as: :date, start_year: Date.today.year - 90,
                           end_year: Date.today.year - 12, order: [:day, :month, :year], input_html: { "data-role" => "none"} %>
    <%= f.input :gender, collection: ["male", "female"] %>
    <%= f.input :location, input_html: { class: 'form-control' }, placeholder: 'Country, City' %>
    <%= f.input :relationship, input_html: { class: 'form-control' }, placeholder: '(single)' %>
  </fieldset>
  </div>

  <div class="col-md-offset-1 col-md-5">
  <fieldset class="form-group">
    <legend>Detailed Information</legend>
    <p class="text-center gray">Hint: Press [enter] for a new paragraph.</p>
    <%= f.input :about, input_html: { class: 'form-control', rows: 3 }, label: 'About you', placeholder: 'I am... I think... I feel...' %>
    <%= f.input :music, input_html: { class: 'form-control', rows: 3 }, label: 'Top favourite Artists' %>
    <%= f.input :character, input_html: { class: 'form-control', rows: 3 }, placeholder: '(moody, kind, forgiving,...)' %>
    <%= f.input :interests, input_html: { class: 'form-control', rows: 3 }, placeholder: '(photography, showboarding, guitars,...' %>
  </fieldset>
  </div>

  <hr class="col-md-12" />

  <div class="col-md-6">
  <fieldset class="form-group">
    <legend>Profile Picture</legend>
    <p class="text-center gray">Avatar must be .jpg / .png / .gif and up to 3mb.</p>
    <%= f.input :avatar, as: :file %>
    <%= bootstrap_paperclip_avatar f, :avatar %>
    <%= f.input :background, as: :file %>
  </fieldset>
  </div>

  <div class="col-md-offset-1 col-md-5">
  <fieldset class="form-group">
    <legend>Account Info</legend>
    <%= f.input :role, disabled: true, input_html: { class: 'form-control' } %>
    <%= f.input :email, disabled: true, input_html: { class: 'form-control' } %>
    <%= f.input :profile_name, disabled: true, input_html: { class: 'form-control'} %>
  </fieldset>
  </div>

  <div class="col-md-12">
  <fieldset class="form-group">
    <legend>Update Password</legend>
    <%= f.input :password, hint: "Leave blank if you do not want to change it", input_html: { class: 'form-control', autocomplete: 'off', value: "" } %>
    <%= f.input :password_confirmation, input_html: { class: 'form-control', autocomplete: 'off' } %>
  </fieldset>
  </div>

  <div class="col-md-12">
  <fieldset class="form-group">
    <legend>Confirm Changes</legend>
    <%= f.input :current_password, hint: "We need your current password to confirm your changes", input_html: { class: 'form-control', autocomplete: 'off' } %>
  </fieldset>
  </div>

  <div class="col-md-12">
  <div class="form-actions">
    <button type="submit" class="btn btn-primary btn-large btn-block">Update</button>
    <%= link_to "Cancel my account", registration_path(resource_name), :data => { :confirm => "Are you sure you want to delete your account? Most of your content may be deleted.", :ajax => 'false' }, :method => :delete, class: 'pull-right' %>
  </div>
  </div>
<% end %>